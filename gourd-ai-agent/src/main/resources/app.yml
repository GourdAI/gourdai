server.port: 4808

server.request:
  maxBodySize: 2mb
  maxFileSize: 2mb

solon.app:
  name: "gwork"
  title: "GWork"

solon.logging.appender:
  console:
    enable: false
  file:
    level: DEBUG
    name: ".gwork/logs/${solon.app.name}"

solon:
  logging:
    logger:
      "org.eclipse.jetty":
        level: ERROR
      "io.modelcontextprotocol":
        level: ERROR
